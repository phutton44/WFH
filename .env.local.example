# Copy to `.env.local` (gitignored) — never commit real secrets.
# Pull from Vercel after linking the project:
#   npx vercel env pull .env.local --environment=development --yes
# If secrets were exposed, rotate them in Vercel, then pull again.
